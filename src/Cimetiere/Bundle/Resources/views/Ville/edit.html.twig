{% extends 'CimetiereBundle::layout.html.twig' %}

{% block contenu -%}
<div class="container-fluid page-container">
    <div class="row">
        <div class="content-tab col-sm-12 col-md-12  col-lg-12">
            <h1>Édition de Ville</h1>

            {{ form_start(edit_form, {'attr': {'class': 'form-horizontal'} }) }}
    {{ form_errors(edit_form) }}

            <div class="form-group">
        {{ form_label(edit_form.nomVille, "Nom de la ville : ", {'label_attr': {'class': 'col-sm-2 control-label'} }) }}
<div class="col-sm-4">
        {{ form_errors(edit_form.nomVille) }}
        {{ form_widget(edit_form.nomVille, {'attr': {'class': 'form-control'} }) }}
                </div>
                </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-4">
        {{ form_end(edit_form , {'attr': {'class': 'btn btn-default'} }) }}
    </div>
          
        
  </div>
  <div class="form-group">
      <div class="col-sm-offset-2 col-sm-4">
        {{ form(delete_form , {'attr': {'class': 'form-horizontal'} }) }}
    </div>
  </div>
                    <ul class="record_actions">
                        <li>
                            <a href="{{ path('ville') }}">
                                <button class="btn btn-success">Retour à la liste</button>
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
{% endblock %}
